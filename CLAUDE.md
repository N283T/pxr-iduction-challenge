# PXR Induction Challenge

Claude should follow the repository operating guide in `AGENTS.md`.

This file intentionally stays small to avoid duplicated, stale experiment state.
Use it only for Claude-specific reminders.

## Claude-Specific Reminders

- Read `AGENTS.md` first.
- Do not quote current leaderboard rank, gap, or best submission from memory. Check the latest `docs/leaderboards/` snapshot and local DB rows.
- Keep detailed experiment notes in GitHub issues, especially issue #100 for Track 1.
- Do not commit ignored local files such as `track1_activity/scripts/api.py`, submissions, checkpoints, embeddings, DB data, or Boltz runtime outputs.
- Run focused local checks for touched files. This repo has no GitHub Actions CI.
