# PXR Induction Challenge

Claude should follow the repository operating guide in `AGENTS.md`.

This file intentionally stays small so agent-specific guidance does not drift
away from the shared rules. Use `AGENTS.md` as the source of truth.

## Claude-Specific Reminders

- Read `AGENTS.md` before making changes.
- Keep responses and code changes aligned with the repository's English-first
  technical documentation convention.
- Do not commit ignored local files such as submissions, checkpoints,
  embeddings, database data, or Boltz runtime outputs.
- Run focused local checks for touched files. This repo has no GitHub Actions CI.
